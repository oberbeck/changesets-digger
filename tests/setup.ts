import { afterEach, beforeEach } from 'vitest';

// Global test setup - no longer needed since each test uses unique temp directories
beforeEach(() => {
  // Individual tests handle their own cleanup
});

afterEach(() => {
  // Individual tests handle their own cleanup
});
